# Interviewer Task Index

The Interviewer work is split intentionally:

- `tasks-lambda.md` is the historical implementation record for the context-builder Lambda.
- `tasks-nova-sonic-conversation.md` is the active AgentCore voice-runtime, authenticated WSS, and integration tracker. Amplify hosts the frontend separately; environment-specific configuration and end-to-end verification are tracked there.
